version: '3.7'

services:
  api:
    volumes: 
      - ./app:/srv
      - ./board/dist:/public
    ports:
      - "${WEB_PORT}:5000"

  migration:
    volumes:
      - ./migration:/srv

  board: &default-node
    volumes:
      - ./board:/srv

  storybook:
    image: "h4nyu/mlboard_board:${TAG}"
    volumes:
      - ./board:/srv
    command: yarn storybook
    ports:
      - "${STORYBOOK_PORT}:9001"

networks:
  default:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${SUBNET}

